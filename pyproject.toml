[project]
name = "zangy"
repository = "https://github.com/Gelbpunkt/zangy"

[build-system]
requires = ["maturin>=0.14,<0.15"]
build-backend = "maturin"

[tool.maturin]
manylinux = "Linux"
strip = true
